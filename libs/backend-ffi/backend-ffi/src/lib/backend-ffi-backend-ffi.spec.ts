import { backendFfiBackendFfi } from './backend-ffi-backend-ffi';

describe('backendFfiBackendFfi', () => {
  it('should work', () => {
    expect(backendFfiBackendFfi()).toEqual('backend-ffi-backend-ffi');
  });
});
